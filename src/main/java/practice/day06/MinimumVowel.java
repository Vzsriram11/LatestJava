package practice.day06;

import java.util.Arrays;
import java.util.List;
import java.util.concurrent.ExecutorService;
import java.util.concurrent.Executors;
import java.util.stream.Collectors;

public class MinimumVowel {

    public  static  void main(String[] args)
    {
        List<String> strings = Arrays.asList("apple", "banana", "kiwi", "orange", "pear");
        //Given a list of strings, write a program
        // to find and print the strings with the minimum number of vowels using Java Stream API.

      //  strings.stream().map(String::toLowerCase).collect(Collectors.toMap())
       // ExecutorService executorService = Executors.newFixedThreadPool(10);
        //executorService.

    }
}
